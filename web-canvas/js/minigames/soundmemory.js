// soundmemory.js — audio pairs matching game (Childsplay: Soundmemory).
// Flip tiles to hear a sound; match the pairs. Assets under
// assets/audio/lib/CPData/SoundmemoryData.
import { makeStub } from './_stub.js';

export default makeStub({
  title: 'Sound Memory',
  blurb: 'Flip the tiles, listen to each sound, and find the matching pairs.',
});
