// memory.js — classic picture pairs game (Childsplay: Memory).
// Tilesets under assets/graphics/lib/CPData/Memory_spData.
import { makeStub } from './_stub.js';

export default makeStub({
  title: 'Memory',
  blurb: 'Turn over two cards at a time and remember where each picture is to make pairs.',
});
