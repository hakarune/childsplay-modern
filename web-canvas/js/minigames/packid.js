// packid.js — maze "eat the dots" chase game (Childsplay: Packid).
// Assets: assets/graphics/lib/CPData/PackidData, assets/audio/lib/CPData/PackidData
import { makeStub } from './_stub.js';

export default makeStub({
  title: 'Packid',
  blurb: 'Guide Packid around the maze and eat every dot before the ghosts catch you.',
});
